# 🚀 Быстрый старт ФинПульс

## Шаг 1: Проверка Python

```bash
python3 --version
# Должна быть версия 3.10+ (минимум 3.9)
```

## Шаг 2: Установка зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/Mac

# Установить пакеты
pip install -r requirements.txt
```

## Шаг 3: Настройка API ключа

1. Скопируйте пример конфигурации:
```bash
cp env.example .env
```

2. Откройте `.env` и вставьте ваш API ключ:
```env
OPEN_ROUTER_API_KEY=ваш_ключ_здесь
```

## Шаг 4: Запуск сервера

```bash
python3 app.py
```

Вы увидите:
```
╔═══════════════════════════════════════════════════════╗
║                 🚀 ФинПульс API                       ║
║                                                       ║
║  Сервер запущен!                                     ║
║  Веб-интерфейс: http://localhost:8000                ║
║  API документация: http://localhost:8000/docs        ║
╚═══════════════════════════════════════════════════════╝
```

## Шаг 5: Открыть в браузере

Откройте: **http://localhost:8000**

## 🎯 Готово!

Теперь вы можете:
- 📊 Анализировать транзакции
- 📄 Проверять документы
- 💬 Задавать вопросы налоговому консультанту

---

## 🔧 Возможные проблемы

### Ошибка: "Module not found"
```bash
pip install -r requirements.txt
```

### Ошибка: "Port 8000 already in use"
Измените порт в `app.py`:
```python
uvicorn.run("app:app", host="0.0.0.0", port=8001)
```

### Ошибка RAG индекса
При первом запуске нужен файл `train_data.csv` в корне проекта. 
Если его нет, создайте пустую заглушку или отключите RAG в консультанте.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что API ключ корректный
3. Посмотрите логи в консоли

---

**Приятного использования! 🎉**

