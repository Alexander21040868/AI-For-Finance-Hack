# 🚀 Быстрый старт ФинПульс

## Шаг 1: Проверка Python

```bash
python3 --version
# Должна быть версия 3.10+ (минимум 3.9)
```

## Шаг 2: Установка зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить пакеты
pip install -r requirements.txt
```

## Шаг 3: Настройка API ключа

1. Создайте файл `.env` в корне проекта (если его нет)

2. Добавьте ваш OpenRouter API ключ:
```env
OPEN_ROUTER_API_KEY=ваш_ключ_здесь
```

**Где получить API ключ:**
- Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
- Создайте API ключ в настройках аккаунта
- Скопируйте ключ в файл `.env`

## Шаг 4: Подготовка базы знаний (опционально)

База знаний для юридического консультанта автоматически генерируется при первом запуске из файлов в `knowledge_base_builder/`.

Если хотите использовать свою базу знаний:
1. Поместите `.txt` файлы с законами/статьями в `knowledge_base_builder/data/`
2. Или используйте веб-скрапинг (настройте в `knowledge_base_builder/scrape_web.py`)
3. Запустите генерацию: `python knowledge_base_builder/build_raw_data.py`

## Шаг 5: Запуск сервера

### Вариант 1: Через app.py (рекомендуется)
```bash
python3 app.py
```

### Вариант 2: Через main.py (альтернативный запуск)
```bash
python3 main.py
```

Вы увидите:
```
╔═══════════════════════════════════════════════════════╗
║                 🚀 ФинПульс API                       ║
║                                                       ║
║  Сервер запущен!                                     ║
║  Веб-интерфейс: http://localhost:8000                ║
║  API документация: http://localhost:8000/docs       ║
║  Health check: http://localhost:8000/health          ║
╚═══════════════════════════════════════════════════════╝
```

## Шаг 6: Открыть в браузере

Откройте: **http://localhost:8000**

Вы увидите современный веб-интерфейс с тремя вкладками:
- 💰 **Умный финансовый аналитик** - анализ транзакций, визуализации, прогнозы
- 📄 **Анализатор документов** - проверка договоров на риски
- ⚖️ **Юридический консультант** - ответы на вопросы по налогам

## 🎯 Готово!

Теперь вы можете:
- 📊 **Анализировать транзакции**: загрузите CSV/XLSX с банковской выпиской
- 📈 **Смотреть визуализации**: графики доходов/расходов, сравнение периодов
- 🔍 **Находить аномалии**: всплески расходов, новые контрагенты
- 📊 **Сравнивать с индустрией**: бенчмаркинг CAC, маржи
- 💼 **Планировать налоги**: годовые прогнозы, сравнение режимов
- 📄 **Проверять документы**: анализ договоров на риски
- 💬 **Задавать вопросы**: консультации по налогам и законодательству

---

## 🔧 Возможные проблемы

### Ошибка: "Module not found"
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### Ошибка: "Port 8000 already in use"
Измените порт в `app.py` (строка 599):
```python
uvicorn.run(
    "app:app",
    host="0.0.0.0",
    port=8001,  # Измените на другой порт
    reload=True
)
```

Или убейте процесс, занимающий порт:
```bash
# Linux/Mac
lsof -ti:8000 | xargs kill -9

# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### Ошибка: "OPEN_ROUTER_API_KEY not found"
1. Убедитесь, что файл `.env` существует в корне проекта
2. Проверьте, что ключ указан правильно (без пробелов, кавычек)
3. Перезапустите сервер

### Ошибка RAG индекса при первом запуске
База знаний генерируется автоматически при первом запуске из `knowledge_base_builder/output/raw_documents.jsonl`.

Если файла нет:
1. Запустите генерацию базы знаний:
   ```bash
   python knowledge_base_builder/build_raw_data.py
   ```
2. Или система создаст пустую базу при первом запросе к консультанту

### Медленная работа LLM
- Проверьте баланс на OpenRouter
- Убедитесь, что используете правильную модель (по умолчанию: `google/gemini-2.5-flash-lite`)
- Для категоризации транзакций используется батчинг - это ускоряет обработку

---

## 📊 Тестирование

### Пример CSV файла для тестирования
Создайте файл `test_transactions.csv`:
```csv
Дата,Назначение платежа,Сумма
2024-10-01,Оплата аренды офиса за октябрь,-50000
2024-10-15,Поступление от ООО "Клиент" за услуги,150000
2024-10-20,Зарплата сотрудникам,-80000
2024-10-25,Реклама в Яндекс.Директ,-20000
```

### Проверка работы API
```bash
# Health check
curl http://localhost:8000/health

# Должен вернуть:
# {"status":"ok","services":{...}}
```

---

## 📞 Поддержка

Если возникли проблемы:
1. ✅ Проверьте, что все зависимости установлены (`pip list`)
2. ✅ Убедитесь, что API ключ корректный (проверьте на OpenRouter)
3. ✅ Посмотрите логи в консоли (там будут сообщения об ошибках)
4. ✅ Проверьте версию Python (`python3 --version` должна быть 3.10+)
5. ✅ Убедитесь, что порт 8000 свободен

---

## 🎓 Дополнительная информация

- 📖 **Полная документация**: см. `README.md`
- 🎬 **Демо-гайд**: см. `DEMO_GUIDE.md`
- 🔌 **API документация**: http://localhost:8000/docs (после запуска)

---

**Приятного использования! 🎉**
